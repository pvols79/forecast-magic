server {
    listen 80;
    listen [::]:80;
    server_name forecast-magic.example.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name forecast-magic.example.com;

    ssl_certificate /etc/letsencrypt/live/forecast-magic.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/forecast-magic.example.com/privkey.pem;

    client_max_body_size 1m;

    location / {
        proxy_pass http://forecast-magic:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
